import { Typography, ViewContent } from '@shared/components';

export default function HomePublicContent() {
  return (
    <ViewContent>
      <Typography variant="title">HomePublicContent</Typography>
    </ViewContent>
  );
}
