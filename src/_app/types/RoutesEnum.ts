export enum MainStackRoutesEnum {
  PUBLIC = '(public)',
  TABS = '(tabs)',
  NOT_FOUND = '+not-found',
}

export enum MainTabsRoutesEnum {
  HOME = '(home)/index',
  EXPLORE = '(explore)/index',
  DEMO = '(demo)/index',
}
