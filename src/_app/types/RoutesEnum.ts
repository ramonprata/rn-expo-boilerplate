export enum MainStackRoutesEnum {
  PUBLIC_LOGIN = 'login',
  PUBLIC_INTRO = 'intro',
  PUBLIC_TERMS = 'terms',
  NOT_FOUND = '+not-found',
}

export enum MainTabsRoutesEnum {
  HOME = 'home',
  EXPLORE = 'explore',
  DEMO = 'demo',
}
